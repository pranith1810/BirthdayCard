package com.example.birthdaycard.ui.main

data class Person(val name:String="") { //used a primary constructor and also initialising properties in the constructor itself which also has a default value
}